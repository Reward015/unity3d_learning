using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrintingToConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello I have no doors or windows, how silly");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
